{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) uv run --quiet ruff format \"$f\" 2>/dev/null || true ;; esac; }"
          }
        ]
      }
    ]
  }
}
