# Neural-network layers & training

The Transformer/positional-encoding building blocks, the PyTorch Geometric temporal
convolution layers used by the ST-GNN, and the training loop.

::: swissrivernetwork.benchmark.transformer

::: swissrivernetwork.benchmark.nn.temporal_gat_conv

::: swissrivernetwork.benchmark.nn.temporal_nn_conv

::: swissrivernetwork.benchmark.training
