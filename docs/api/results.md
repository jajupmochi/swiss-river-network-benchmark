# Results & tables export

Rebuild the paper's result tables and figures from the committed CSVs — every per-station
statistic (mean, median, min, max, std, CI), a JSON export, a single `.tex`, and a
tables-only PDF (Tier 0 reproduction).

::: swissrivernetwork.benchmark.visualize_results.latex_tables_export
